import React from 'react';
import ReactDOM from 'react-dom/client';

const heading = <div>
    <h1>Welcome to Simuleduco Solutions</h1>
    <span>Welcome to React Workshop</span>
</div>;

const root = ReactDOM.createRoot(document.getElementById('root'));

root.render(heading);